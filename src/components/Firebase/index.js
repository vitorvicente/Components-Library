import FirebaseContext, { withFirebase } from "./context";
import Firebase, { getFirebase, setFirebaseClass } from "./firebase";

export default Firebase;

export { FirebaseContext, withFirebase, getFirebase, setFirebaseClass };