import React from 'react';

import { VtrHeader } from '../components/VtrHeader';

export default {
  title: 'Vtr/VtrHeader',
  component: VtrHeader,
};

const Template = () => <VtrHeader />;

export const Base = Template.bind({});