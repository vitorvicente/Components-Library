import React from 'react';

import { VtrFooter } from '../components/VtrFooter';

export default {
  title: 'Vtr/VtrFooter',
  component: VtrFooter,
};

const Template = () => <VtrFooter />;

export const Base = Template.bind({});